{
  "name": "workflow-poc",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "workflow": "tsx src/runner.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  }
}
